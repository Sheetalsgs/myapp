apiVersion: apps/v1
kind: Deployment
metadata:
  name: nodeapp-deployment
  labels:
    app: nodeapp
spec:
  replicas: 2
  selector:
    matchLabels:
      app: nodeapp
  template:
    metadata:
      labels:
        app: nodeapp 
    spec:
      imagePullSecrets:
      - name: regcred
      containers:
      - name: nodeserver
        image: sheetalshevalkar/test-project:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 3000

